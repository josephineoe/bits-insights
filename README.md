# bits-insights
Leetcode dream team.

## Setup Instructions

### 1. Make sure Docker Desktop is running  
- Windows/macOS: open **Docker Desktop** (whale icon should show "running").  
- Linux: ensure Docker is started (`systemctl start docker`).

### 2. Clone the repo
```bash
git clone <repo_url>
cd bits-insights

docker compose up --build